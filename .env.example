# Optional: override the UI-configured connection via environment variables.
# If both are set, they take priority over anything saved through the web UI.
# Leave blank (or omit the file entirely) to configure via the web UI instead.
DISPATCHARR_URL=
DISPATCHARR_TOKEN=
